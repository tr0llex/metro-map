name: Deploy

# Механика выкатки — в deploy-kit. Здесь только что катим и какой регресс
# обязан пройти.
#
# Триггер общий для всех проектов samoy.love: мерж в main уезжает на прод.
#
# Раньше здесь стоял только ручной запуск, и довод был такой: приложение
# раздаётся офлайн-PWA, у пользователей с установленным service worker новая
# версия подхватывается по своим правилам, значит выкатка должна быть решением.
# Но это описывает поведение service worker, а не момент выкатки: когда клиент
# увидит новую версию, определяет он, а не то, нажали кнопку руками или пришёл
# мерж. Взамен main переставал быть выкатываемым — ровно то состояние, из-за
# которого «что сейчас на проде» приходится выяснять, а не знать.

on:
  push:
    branches: [main]
    paths-ignore:
      # Шаблон '**.md' здесь безопасен, но безопасен он не сам по себе:
      # markdown в этом репозитории — только README и docs/, ни один .md не
      # читается сборкой. Копировать эту строку в репозиторий, где текст
      # страниц лежит в markdown, нельзя: там шаблон спрячет от выкатки
      # правку самого прода.
      - '**.md'
      # docs/ — документация, эталоны visual-qa (два десятка PNG) и
      # docs/metro-reference/*.json. Справочник по линиям держат для сверки
      # руками: в src/ на него нет ни одной ссылки, данные карты идут из
      # data/ и normalized/. Обновление эталонов снимков и справочника не
      # меняет dist, и гонять из-за них прод незачем.
      - 'docs/**'
      # Настройки редактора в гейт не входят: eslint их не читает, prettier
      # в зависимостях нет.
      - '.editorconfig'
  workflow_dispatch:
    inputs:
      dry-run:
        description: 'Показать план, ничего не менять'
        type: boolean
        default: false

# Минимальные права для GITHUB_TOKEN. Выкатка ходит на сервер по SSH
# секретами, токен ей нужен только для checkout.
permissions:
  contents: read

jobs:
  deploy:
    uses: tr0llex/deploy-kit/.github/workflows/static-site.yml@main
    with:
      config: .deploy-kit/prod.env
      # nginx-conf сознательно не передаём: конфигурация nginx живёт только в
      # deploy-kit и применяется там же. Путь в этом репозитории указывать
      # некуда — deploy/ здесь нет.
      # Полный регресс: типы, линт, токены CSS, тесты с покрытием, сборка и
      # проверка, что редактор не попал в прод-бандл. Красный = выкатки нет.
      gates: >-
        npm ci &&
        npx tsc -b &&
        npm run lint &&
        npm run check:css &&
        npx vitest run --coverage &&
        npm run build &&
        npm run check:bundle
      dry-run: ${{ inputs.dry-run == true }}
    secrets: inherit
