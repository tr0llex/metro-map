# Hello Kitty Метро Москва

Розовое офлайн‑PWA для построения маршрутов в московском метро во вселенной Hello Kitty.

Цель проекта — получить схему уровня Яндекс.Метро: октолинейная геометрия, аккуратные кольца и радиалы, корректные пересадочные хабы и удобный интерфейс маршрутизации, работающий полностью офлайн.

---

## 1. Технологический стек

- **Фронтенд:** React + TypeScript + Vite  
- **Рендер карты:** Canvas 2D ([MetroMap.tsx](src/components/MetroMap.tsx))  
- **PWA:** `vite-plugin-pwa` (авто‑обновляемый service worker, offline‑режим)  
- **Данные метро:** новый датасет из `metro.ru.csv` и `connections.json`  
- **Node‑скрипты:** TypeScript (ESM) для оффлайн‑пайплайна (`scripts/*.ts`)

---

## 2. Быстрый старт

```bash
npm install

# Дев‑режим (Vite)
npm run dev

# Сборка и предпросмотр продакшн‑версии
npm run build
npm run preview

# Пересобрать полный граф и схему
npm run build:data        # пересобрать граф из новых данных
npx ts-node --transpile-only scripts/analyze_layout_advanced.ts  # анализ качества схемы
```

> **Важно:** UI использует предрасчитанные координаты `layoutX/layoutY` из [normalized/fullGraph.json](normalized/fullGraph.json). После любых правок layout‑алгоритма обязательно запускайте пересборку графа и анализ.

---

## 3. Пайплайн данных метро

### 3.1. Источники данных

**Новый пайплайн (текущий):**

- **`new_map_source/metro.ru.csv`** — основной датасет со всеми линиями и станциями метро и МЦК
  - Содержит: `cityId`, `cityName`, `lineId`, `lineName`, `lineColorHex`, `stationNumericId`, `stationName`, `lat`, `lng`, `order`
  - Включает все линии метро, МЦК и БКЛ
  - Исключает МЦД (D1-D4)
  
- **`new_map_source/connections.json`** — данные о всех пересадках между станциями
  - Содержит: типы соединений (`interchange`, `cross_platform`, `mcc`, `out-of-station`)
  - Включает все типы пересадок, включая `out-of-station`

### 3.2. Построение полного графа

- **Скрипт:** Go-решатель [go-layout-solver](go-layout-solver) (см. `go-layout-solver/main.go`)  
- **Вход:** `new_map_source/metro.ru.csv`, `new_map_source/connections.json`  
- **Выход:** [normalized/fullGraph.json](normalized/fullGraph.json)

**Основные шаги:**

1. **Загрузка данных** из CSV и JSON
2. **Фильтрация** — только Москва, исключение МЦД
3. **Проекция координат** — `lat/lng` → `layoutX/layoutY` (Web Mercator проекция)
4. **Построение линий** — создание `FullGraphLine` для каждой линии
5. **Построение станций** — создание `FullGraphStation` с координатами
6. **Построение рёбер** — связи между соседними станциями на линиях
7. **Построение хабов** — группировка станций по пересадкам из `connections.json`
8. **Layout оптимизация** — последовательность алгоритмов:
   - Построение колец (Кольцевая, МЦК, БКЛ) с увеличенными радиусами
   - Снап всех хабов к кольцевым линиям (приоритет)
   - Раздвижение станций внутри кольцевой линии
   - Сглаживание не-кольцевых линий
   - Итеративная оптимизация позиций станций
   - Финальное масштабирование карты

**Структуры данных:**

```ts
interface FullGraphLine {
  id: number
  title: string
  colorHex: string
  stationIds: string[]
}

interface FullGraphStation {
  id: string
  title: string
  lineNumericId: number | null
  isTransfer: boolean
  hubId?: string
  layoutX?: number
  layoutY?: number
  lat?: number
  lon?: number
}

interface FullGraphEdge {
  fromStationId: string
  toStationId: string
  lineNumericId?: number
  medianTravelSeconds: number
  isTransfer?: boolean
}

interface FullGraphTransferHub {
  id: string
  stationIds: string[]
  minTransferSeconds: number
}
```

### 3.3. Особенности нового пайплайна

- ✅ **Все станции и линии** метро и МЦК включены
- ✅ **Все пересадки** обработаны, включая `out-of-station`
- ✅ **Все хабы компактные** — станции в одном хабе принудительно слипаются
- ✅ **Кольца выпуклые** — Кольцевая, МЦК и БКЛ всегда выпуклые многоугольники
- ✅ **Координаты из данных** — используется реальная проекция координат

---

## 4. Алгоритм построения схемы

### 4.1. Порядок операций

1. **Построение колец** (`enforceRingConvexity`)
   - Кольцевая линия (id=5): радиус ×7
   - МЦК (id=95): радиус ×6
   - БКЛ (id=97): радиус ×7.5
   - Все станции кольца размещаются на окружности

2. **Снап всех хабов** (`snapAllTransferHubs`)
   - Приоритет кольцевым линиям
   - Все станции в хабе получают координаты якорной станции
   - Результат: все хабы компактные (0px расстояние между станциями)

3. **Раздвижение внутренних станций** (`explodeInnerStationsAroundRing`)
   - Агрессивное раздвижение станций внутри Кольцевой линии
   - Фактор раздвижения: 7-13 (настраивается)
   - Зона раздвижения: до 95-98% радиуса кольца

4. **Сглаживание линий** (`smoothNonRingLines`)
   - Детекция резких поворотов (<135°)
   - Агрессивное сглаживание для резких углов
   - Многократные итерации (12-20)

5. **Оптимизация позиций** (`optimizeStationPositions`)
   - Раздвижение слишком близких станций
   - Минимальное расстояние: 50px
   - Критические зоны: <30px (агрессивное раздвижение), <15px (экстремальное)
   - Многократные итерации (50-60)

6. **Финальное масштабирование** (`scaleEntireMap`)
   - Целевой размер: 2200×1700px
   - Отступы: 150px
   - Сохранение пропорций

### 4.2. Параметры алгоритма

**Текущие оптимальные параметры** (найдены автоматическим поиском):

```json
{
  "koltsevayaRadiusScale": 7,
  "mccRadiusScale": 6,
  "bklRadiusScale": 7.5,
  "innerMaxRatio": 0.55,
  "innerBorderRatio": 0.95,
  "innerExplosionFactor": 7,
  "minDistance": 50,
  "criticalDistance": 30,
  "extremeDistance": 15,
  "smoothingIterations1": 12,
  "optimizationIterations": 50,
  "smoothingIterations2": 10
}
```

См. [normalized/best_params.json](normalized/best_params.json) для актуальных значений.

---

## 5. Система оптимизации параметров

### 5.1. Анализ качества схемы

**Скрипт:** [scripts/analyze_layout_advanced.ts](scripts/analyze_layout_advanced.ts)

**Метрики:**

- **Кольца:**
  - Радиус каждого кольца
  - Эксцентриситет (отклонение от идеальной окружности)
  
- **Расстояния:**
  - Среднее расстояние между станциями
  - Минимальное расстояние
  - Количество близких пар (<40px, <20px)
  - Плотность внутри и снаружи кольца

- **Геометрия:**
  - Количество резких поворотов (>120°)
  - Средний и максимальный углы поворота
  - Октолинейность (количество идеальных сегментов)
  - Гладкость каждой линии

- **Хабы:**
  - Компактность каждого хаба (расстояние между станциями)
  - Количество некомпактных хабов

- **Общий скоринг** — взвешенная сумма всех метрик

**Использование:**

```bash
npx ts-node --transpile-only scripts/analyze_layout_advanced.ts
```

Результаты сохраняются в [normalized/layout_metrics.json](normalized/layout_metrics.json).

---

## 6. Архитектура рантайма (`src/`)

### 6.1. Точка входа и оболочка

- `src/main.tsx` — bootstrap React/Vite  
- `src/index.css` — базовые стили  
- `src/App.tsx` — главный компонент:
  - управляет выбором станций «Откуда/Куда»
  - вызывает `findRouteAlternativesFullGraph` из `routing.ts` (альтернативные маршруты по полному графу)
  - передаёт `routeStationIds` и `routeEdgeKeys` в [MetroMap](src/components/MetroMap.tsx) для подсветки маршрута
  - включает режим редактора для ручной правки координат, параметров хабов и рёбер

### 6.2. Модуль метро (`src/metro`)

- **`types.ts`** — базовые типы для линий, станций, рёбер, хабов, маршрутов
- **`fullGraph.ts`** — импорт `normalized/fullGraph.json`
- **`graphCore.ts`** — низкоуровневые функции работы с графом (`undirectedEdgeKey`, `buildAdjacencyListFromFullGraph`, поиск кратчайшего пути, сборка `RouteResult`). Константа `TRANSFER_PENALTY_MINUTES` теперь равна `0` — искусственный штраф за пересадки по умолчанию отключён.
- **`routing.ts`** — высокоуровневый API маршрутизатора (`findShortestRouteFullGraph`, `findRouteAlternativesFullGraph`), использует `graphCore` и полный граф, поддерживает оверрайды рёбер и дополнительные рёбра редактора.
- **`layoutEngine.ts`** — fallback‑алгоритм layout'а (если нет `layoutX/layoutY`)

### 6.3. Компонент MetroMap (Canvas‑схема)

**Файл:** [src/components/MetroMap.tsx](src/components/MetroMap.tsx)

**Функциональность:**

- Загрузка `fullGraphLines` и `fullGraphStations`
- Построение `positionedStations` из `layoutX/layoutY` или fallback
- Рендер на Canvas:
  - Линии — полилинии через все станции линии (кольца замкнуты)
  - Маршрут — подсветка пути по `routeEdgeKeys`
  - Станции — кружки с обводкой в цвет линии
  - Подписи — размещение с коллизионным алгоритмом
- Интерактив:
  - Pan/zoom (мышь, колесо, touch)
  - Клик на станцию для выбора
  - Режим редактора — drag & drop станций и подписей
- Viewport:
  - Автоматическое центрирование и масштабирование при загрузке
  - Ограничение pan/zoom границами карты
  - Zoom относительно курсора мыши

---

## 7. Текущее состояние проекта

### 7.1. Статистика данных

Актуальные числа линий, станций, рёбер и хабов берутся из [normalized/fullGraph.json](normalized/fullGraph.json) и зависят от входных данных `metro.ru.csv`/`connections.json` и параметров layout‑алгоритма. Сейчас в графе описана полная сеть метро Москвы и МЦК (МЦД по‑прежнему исключены).

### 7.2. Текущие метрики качества

**Последний анализ** ([normalized/layout_metrics.json](normalized/layout_metrics.json)) показывает, что:

- ✅ **Кольца:**
  - Кольцевая: радиус ≈275px, эксцентриситет ≈5.9%
  - МЦК: радиус ≈496px, эксцентриситет ≈11.7%
  - БКЛ: радиус ≈540px, эксцентриситет ≈16.4% (нужно улучшать)

- ⚠️ **Расстояния:**
  - Минимальное расстояние между станциями: ≈0.9px (критично мало по сравнению с целевыми 50px)
  - Близких пар (`closePairsCount`): 121
  - Очень близких пар (`veryClosePairsCount`): 76

- ✅ **Геометрия линий:**
  - Резких поворотов (`sharpTurnsCount`): 9 (в пределах целевого <10)
  - Средний угол поворота ≈22.7°, максимальный ≈179.3°

- ⚠️ **Хабы и компактность:**
  - Часть хабов остаётся не снапнутой (`hubsNotSnapped`: 22)

- **Общий скоринг:** `overallScore ≈ 1228` (см. `layout_metrics.json` для точного значения и деталей по линиям/хабам).

### 7.3. Известные проблемы

1. **Слишком близкие станции** — минимальное расстояние < 1px вместо желаемых 50px; много «очень близких» пар.
2. **Неидеальные кольца (особенно БКЛ)** — эксцентриситет БКЛ значительно выше целевых 5%, МЦК тоже требует доработки.
3. **Частично неснапнутые хабы** — `hubsNotSnapped` > 0, часть пересадок визуально выглядит разорванной.
4. **Нерегулярное распределение станций** — центр остаётся перегруженным по плотности, на периферии есть провалы (см. `innerRingDensity`/`outerRingDensity`).

### 7.4. Приоритетные задачи

**HIGH:**
- Увеличить минимальное расстояние между станциями до 50px и сократить число очень близких пар
- Улучшить компактность хабов (добиться `hubsNotSnapped = 0`)
- Снизить эксцентриситет БКЛ и МЦК до целевых значений
- Сохранить/поддерживать число резких поворотов на уровне <10 и улучшать гладкость линий

**MEDIUM:**
- Продолжить итеративную оптимизацию параметров
- Улучшить алгоритм раздвижения станций
- Добавить больше итераций сглаживания

**LOW:**
- Полировка UI
- Hello Kitty стиль
- Дополнительные фичи

---

## 8. Файловая структура

### 8.1. Данные

- **`new_map_source/`**
  - `metro.ru.csv` — основной датасет станций и линий
  - `connections.json` — данные о пересадках

- **`normalized/`**
  - `fullGraph.json` — полный граф с координатами (основной файл для приложения)
  - `layout_metrics.json` — метрики качества схемы
  - `best_params.json` — лучшие найденные параметры оптимизации

### 8.2. Скрипты

- **`go-layout-solver/`** — Go-решатель построения графа из `metro.ru.csv` и `connections.json`
- **`scripts/extract_yandex_coords.ts`** — парсер HTML Яндекс-схемы в `normalized/yandex_coords.json`
- **`scripts/analyze_layout_advanced.ts`** — расширенный анализ качества схемы

### 8.3. Исходный код

- **`src/components/MetroMap.tsx`** — Canvas‑компонент карты
- **`src/metro/`** — модули данных и логики метро
- **`src/App.tsx`** — главный компонент приложения

### 8.4. Документация

- **`README.md`** (этот файл) — основная документация
- **`OPTIMIZATION_SYSTEM.md`** — документация системы оптимизации
- **`ALGORITHM_IMPROVEMENTS.md`** — история улучшений алгоритма

---

## 9. Рабочий процесс разработки

### 9.1. Изменение данных

1. Обновить `new_map_source/metro.ru.csv` или `new_map_source/connections.json`
2. Запустить пересборку:
   ```bash
   npm run build:data
   ```
3. Проверить результаты:
   ```bash
   npx ts-node --transpile-only scripts/analyze_layout_advanced.ts
   ```

### 9.2. Изменение алгоритма layout

1. Изменить параметры в Go-решателе (`go-layout-solver/layout.go` и/или `go-layout-solver/graph.go`)
2. Пересобрать граф:
   ```bash
   npm run build:data
   ```
3. Проанализировать:
   ```bash
   npx ts-node --transpile-only scripts/analyze_layout_advanced.ts
   ```
---

## 10. Дорожная карта

### 10.1. Выполнено ✅

- ✅ Создан новый пайплайн данных из `metro.ru.csv` и `connections.json`
- ✅ Все хабы сделаны компактными (0px расстояние)
- ✅ Кольца строятся первыми с увеличенными радиусами
- ✅ Расширенный анализ качества схемы
- ✅ Режим редактора для ручной правки координат
- ✅ Viewport с автоматическим центрированием и ограничениями

### 10.2. В работе 🚧

- 🚧 Улучшение минимального расстояния между станциями (текущее: 8.8px, цель: 50px)
- 🚧 Уменьшение количества резких поворотов (текущее: 35, цель: <10)
- 🚧 Улучшение сглаживания линий
- 🚧 Исправление эксцентриситета БКЛ

### 10.3. Планируется 📋

- 📋 Глобальный энергетический решатель (simulated annealing)
- 📋 Альтернативные маршруты
- 📋 Hello Kitty полировка UI
- 📋 PWA улучшения
- 📋 i18n поддержка

---

## 11. Спецификация схемы (ТЗ)

### 11.1. Цели и принципы

Визуальный ориентир — **Яндекс.Метро**.

**Принципы:**

- **Октолинейность:** предпочтение углам 0°, 45°, 90°, 135°
- **Реальные кольца:** Кольцевая, МЦК и БКЛ — выпуклые многоугольники
- **Радиалы как хорды:** пересекают кольца в точках пересадок
- **Корректные хабы:** несколько станций в одном узле сливаются визуально
- **Минимум поворотов:** избегать резких углов (>120°)
- **Читаемость:** минимум пересечений, читаемые подписи
- **Минимальное расстояние:** станции не ближе 50px

### 11.2. Текущие ограничения

- Минимальное расстояние: 50px (желательно, но не всегда достигается)
- Резкие повороты: <10 (цель)
- Эксцентриситет колец: <5% (для Кольцевой и МЦК достигнуто)

---

---

## 12. Детальный беклог и требования к приложению

### 12.1. Общая приоритизация

- **HIGH (критический путь)** — то, без чего приложение не выполняет базовую функцию: читаемая схема, стабильный Canvas, основной UX, маршруты по полному графу.
- **MEDIUM** — улучшения качества схемы и UX, альтернативные маршруты, структуризация кода.
- **LOW** — полировка (Hello Kitty‑стиль, PWA‑мелочи, i18n, избранное, аналитика).

### 12.2. Блок A — Основная карта и маршруты (HIGH)

#### ✅ **implement-interactive-map** — _HIGH, completed_
- **Итог:** схема ощущается «живой» на всех устройствах: плавный pan/zoom, корректный hit‑test по станциям, отсутствие глюков с DPI и размерами.
- **Выполнено:**
  - ✅ Обработка мыши/тача с предсказуемым поведением
  - ✅ Корректная работа DPR (retina) и resize: Canvas всегда sharp
  - ✅ Автоматическое центрирование и масштабирование при загрузке
  - ✅ Ограничение pan/zoom границами карты
  - ✅ Zoom относительно курсора мыши

#### ✅ **draw-route-on-map** — _HIGH, completed_
- **Итог:** пользователь всегда видит, как именно он поедет: маршрут выделен поверх линий, пересадки очевидны, нет «обрывов» линий.
- **Выполнено:**
  - ✅ Подсветка маршрута по `routeEdgeKeys` из `findShortestRouteFullGraph`
  - ✅ Рисование утолщённого пути с аккуратными стыками в хабах
  - ✅ Корректная обработка нескольких участков на одной линии и пересадок

#### ✅ **transfer-hub-modeling** — _HIGH, completed_
- **Итог:** пересадки выглядят как единые узлы; пользователь не видит «россыпь» станций с похожими названиями и не путается, где именно происходит пересадка.
- **Выполнено:**
  - ✅ Формат `transferHubs` в `fullGraph.json` с возможностью объединять несколько станций
  - ✅ `snapAllTransferHubs` — все станции хаба реально сходятся в одну точку
  - ✅ Все хабы компактные (0px расстояние)

#### 🚧 **refactor-map-core** — _MEDIUM→HIGH, pending_
- **Итог:** ядро схемы (layout + геометрия + слои) изолировано от React‑компонента, можно менять алгоритм layout'а, не ломая `MetroMap.tsx` и UI.
- **Работы:**
  - Выделить чистые функции layout'а (`layoutEngine`, потенциальный `mapCore`) из `MetroMap.tsx`
  - Свести `MetroMap` к thin‑view: получение уже подготовленных `positionedStations` + отрисовка
  - Подумать над API уровня «map model», к которому будут обращаться и оффлайн‑скрипты, и рантайм

#### ✅ **fix-canvas-geometry** — _HIGH, completed_
- **Итог:** Canvas всегда совпадает с размерами контейнера, схема не растягивается/не плющится, pan/zoom ведут себя одинаково на всех девайсах.
- **Выполнено:**
  - ✅ Размер Canvas с учётом DPR (внутренние пиксели и CSS‑размер)
  - ✅ Центрирование схемы и предсказуемое смещение при pan
  - ✅ Безопасные пределы зума и pan, чтобы карта не «улетала»

#### 🚧 **station-labels-and-collision** — _HIGH, in_progress_
- **Итог:** подписи читаемы, не перекрывают станции и друг друга, особенно в центре; на периферии видны почти все станции.
- **Работы:**
  - Довести текущий коллизионный алгоритм (кандидатные позиции + скоринг) до стабильного состояния
  - Сбалансировать приоритеты центральных станций и окраин
  - Минимизировать пересечения с кружками станций и линиями
  - Поддержать адекватную отрисовку длинных имён (обрезка/перенос по необходимости)

#### 🚧 **transfer-visual-redesign** — _HIGH, pending_
- **Итог:** пересадочные узлы выглядят как осмысленные «пироги» с секторами по цветам линий; линии действительно пересекаются в хабе, не ломаются и не теряют контакт со станциями.
- **Работы:**
  - Спроектировать визуальный паттерн хаба: внешний радиус, толщина кольца, сектора/лепестки по линиям
  - Определить правила локальной геометрии в окрестности хаба: где обрываются/пересекаются линии, как они входят и выходят
  - Обеспечить, чтобы для всех `transferHubs` линии пересекались в одной точке (центре хаба), а не проходили «рядом»
  - Учитывать порядок линий при отрисовке (общий коридор, затем расхождение), чтобы избежать визуальной «ломки» линий

#### 📋 **labels-separate-layer** — _MEDIUM, pending_
- **Итог:** подписи живут на отдельном слое, поэтому не приходится перерисовывать текст при каждом маленьком движении карты; производительность выше.
- **Работы:**
  - Выделить отдельный Canvas/DOM‑слой для текста
  - Синхронизировать матрицу трансформации (scale/translate) между слоями
  - Разнести частоту рендера: линии/станции — чаще, подписи — реже

#### 🚧 **improve-map-layout** — _MEDIUM→HIGH, in_progress_
- **Итог:** вся сеть читается на одном мобильном экране; в центре нет нечитаемого месива, на периферии нет слишком редких/кривых веток.
- **Работы:**
  - Использовать возможности алгоритма оптимизации для разгрузки центра
  - Исправлять случаи, когда станции оказываются заметно в стороне от линий
  - Улучшить минимальное расстояние между станциями до 50px (текущее: 8.8px)
  - Уменьшить количество резких поворотов до <10 (текущее: 35)

### 12.3. Блок B — Layout‑движок и качество схемы

####  **layout-algorithm-overhaul** — _HIGH, in_progress_
- **Итог:** устойчивый октолинейный layout «кольца + хорды», близкий по логике к Яндекс.Метро, без подков и странных заворотов.
- **Работы:**
  - Формализовать правила: допустимые углы, максимальная суммарная кривизна ветки, ограничения на возврат назад
  - Доработать функции оптимизации в Go-решателе (`go-layout-solver/layout.go` и/или `go-layout-solver/graph.go`)
  - Ввести систему якорей для ключевых узлов и внешних станций
  - Улучшить сглаживание линий для устранения резких поворотов

####  **radial-layout-optimizer** — _MEDIUM, in_progress_
- **Итог:** радиальные линии логично расходятся от колец и друг от друга; линии с общими станциями идут рядом только там, где реально делят коридор.
- **Работы:**
  - Разработать стратегию привязки радиалов к кольцам (хорды через хабы, а не лучи из центра)
  - Реализовать правила «разведения» веток после общих участков
  - Проверять, что изменение радиалов не ломает корректность хабов и колец

#### 📋 **octilinear-layout-experiments** — _LOW, pending_
- **Итог:** понимание, насколько строгий octilinear‑layout улучшает/ухудшает читаемость, и прототип решения на части сети.
- **Работы:**
  - Прототипирование snap'а вершин к 0/45/90/135°
  - Тестирование на подмножестве линий (например, пара радиалов + кольцо)
  - Оценка влияния на длину маршрутов и визуальную сложность

#### 📋 **precise-layout** — _LOW, pending_
- **Итог:** ключевые хабы и внешние участки визуально очень близки к Яндекс.Метро (или другой эталонной схеме).
- **Работы:**
  - Подбор и фиксация anchors для сложных хабов (Киевская, Курская, Китай‑город и т.п.)
  - Ручная правка радиусов/углов на чувствительных участках
  - Возможный экспорт/импорт anchor‑конфига для ручного редактирования

#### 📋 **global-layout-energy-solver** — _HIGH, pending_
- **Итог:** есть глобальный решатель, который минимизирует функцию качества схемы `E(layout)` и позволяет получать существенно более чистый layout за счёт долгого оффлайн‑просчёта.
- **Работы:**
  - Определить функцию энергии `E(layout)` как взвешенную сумму штрафов/бонусов:
    - `E_octilinear` — штраф за отклонение углов сегментов от 0/45/90/135°
    - `E_bend` — суммарная кривизна линии (сумма квадратов углов поворота между сегментами)
    - `E_cross` — штраф за пересечения линий вне хабов
    - `E_spacing` — штраф за слишком маленькие/слишком большие расстояния между соседними станциями
    - `E_ring` — отклонение точек кольцевых линий от идеальной окружности
    - `E_anchor` — отклонение избранных станций/хабов от их anchors
    - `E_corridor` — штраф за неправильное поведение «общих коридоров»
  - Выбрать схему оптимизации: симуляция отжига / iterated local search / гибрид
  - Реализовать генератор «мелких шагов» (движков): локальные сдвиги хабов, изменения радиусов колец, повороты веток, поправки anchors
  - Реализовать быстрый пересчёт локальных вкладов `E` при маленьком изменении layout'а
  - Добавить сохранение лучших найденных конфигураций и логов вклада отдельных членов `E` для анализа

### 12.4. Блок C — Основной UX/экран

#### ✅ **map-as-main-background** — _HIGH, completed_
- **Итог:** пользователь всегда видит карту как фон, UI‑элементы не «отъедают» значимую часть экрана.
- **Выполнено:**
  - ✅ Canvas/слои карты под основными UI‑компонентами
  - ✅ UI элементы не перекрывают карту

#### 📋 **bottom-sheet-station-form** — _HIGH, pending_
- **Итог:** удобный ввод «Откуда/Куда» на мобильном, без перегрузки экрана; легко изменить направление маршрута.
- **Работы:**
  - Реализовать bottom‑sheet с полями поиска станций и подсказками
  - Синхронизировать выбор в форме и клики по схеме
  - Добавить управление фокусом и клавиатурой (особенно на мобильном)

#### 📋 **route-summary-and-sheet** — _HIGH, pending_
- **Итог:** пользователь видит короткое резюме маршрута (время, пересадки) и по желанию раскрывает подробный список шагов.
- **Работы:**
  - Добавить верхнее саммари (badge с временем, количеством пересадок)
  - Реализовать шторку с шагами (станция → станция, с указанием линии/пересадки)
  - Связать прокрутку шторки с подсветкой соответствующих сегментов на карте

#### � **route-arrival-time** — _MEDIUM, pending_
- **Итог:** пользователь видит не только длительность маршрута, но и ориентировочное время прибытия на конечную станцию.
- **Работы:**
  - В рантайме рассчитывать время прибытия `arrivalTime = now + totalMinutes` для активного маршрута
  - Отображать `arrivalTime` в `RouteDetailsSheet` рядом с общей длительностью маршрута
  - Обновлять значение при смене активного маршрута

#### �🚧 **responsive-layout-fix** — _HIGH, in_progress_
- **Итог:** одно и то же приложение адекватно выглядит на телефоне, планшете, десктопе и в PWA‑режиме без визуальных скачков.
- **Работы:**
  - Проработать breakpoints и поведение контейнеров
  - Устранить «прыжки» высоты/ширины при загрузке и смене ориентации
  - Проверить сценарии установки на главный экран (Android/iOS) и десктопный PWA

#### 📋 **split-app-into-components** — _MEDIUM, pending_
- **Итог:** код `App.tsx` читаемый и легко расширяемый; появление новых экранов/панелей не ведёт к взрыву сложности.
- **Работы:**
  - Выделить компоненты: Shell/Layout, MapContainer, StationForm, RouteSummary, RouteSheet и т.п.
  - Ввести простой state‑management (контекст/хук), чтобы компоненты не были жёстко связаны друг с другом

### 12.5. Блок D — Маршрутизация и данные

#### ✅ **unify-routing-with-full-network** — _HIGH, completed_
- **Итог:** маршрутизация в рантайме полностью использует `fullGraph.json`; упрощённый граф и старый маршрутизатор удалены.
- **Выполнено:**
  - ✅ Все вызовы маршрутизатора в UI используют `findRouteAlternativesFullGraph` / `findShortestRouteFullGraph`
  - ✅ Старый маршрутизатор и упрощённая сеть (`network.ts`, `findShortestRoute`) удалены из кода
  - ✅ `fullGraph.json` является единственным источником данных о графе метро во всём приложении

#### ✅ **design-full-graph-schema** — _MEDIUM, completed_
- **Итог:** структура `fullGraph.json` стабилизирована и понятна, пригодна для внешнего ETL.
- **Выполнено:**
  - ✅ Определён состав полей линий, станций, рёбер, хабов
  - ✅ Согласованы с типами в `src/metro/types.ts`
  - ✅ Создан новый пайплайн из `metro.ru.csv` и `connections.json`

#### 📋 **gtfs-etl-pipeline** — _MEDIUM, pending_
- **Итог:** есть воспроизводимый скрипт, который берёт GTFS/OSM и обновляет `fullGraph.json` (или промежуточную структуру), в том числе времена перегонов и пересадок.
- **Работы:**
  - Спарсить GTFS/OSM и сопоставить их с текущими ID станций/линий
  - Сгенерировать travel‑times и transfer‑times
  - Встроить это в существующий пайплайн `new_map_source/*` → `fullGraph.json`

#### 📋 **add-core-tests** — _LOW, pending_
- **Итог:** критические части пайплайна и маршрутизатора покрыты базовыми тестами, чтобы не ломать их случайными изменениями.
- **Работы:**
  - Написать unit‑тесты для построения графа (`build_full_graph_from_metro_ru.ts`)
  - Написать тесты для маршрутизатора (простые проверочные маршруты, пограничные кейсы)

#### ✅ **alt-routes** — _MEDIUM, completed_
- **Итог:** пользователь может выбрать из нескольких разумных маршрутов (самый быстрый, с меньшим количеством пересадок и т.п.).
- **Выполнено:**
  - ✅ `findRouteAlternativesFullGraph` возвращает набор альтернатив с разным штрафом за пересадки
  - ✅ В UI есть чипы/карусель маршрутов (мобайл и десктоп) в `RouteDetailsSheet`
  - ✅ Подсветка маршрута на карте переключается вместе с выбором варианта
- **Дальнейшие улучшения (при необходимости):**
  - уточнить сигнатуры и подписи вариантов (например, всегда показывать «Самый быстрый» / «Минимум пересадок»)
  - приоритизировать работу над качеством альтернатив отдельно от базового P0-функционала

### 12.6. Блок E — Дополнительные фичи и полировка

#### 🚧 **hello-kitty-polish** — _LOW, in_progress_
- **Итог:** приложение ощущается цельным Hello Kitty‑опытом: цвета, иконки, фон, мелкие радости.
- **Работы:**
  - Доработка цветовой палитры, фона и иконок (включая PWA‑иконки)
  - Добавление лёгких анимаций/hover‑состояний без потерь производительности

#### 📋 **pwa-polish** — _LOW, pending_
- **Итог:** PWA корректно устанавливается, имеет красивые иконки/сплэш и предсказуемо обновляется.
- **Работы:**
  - Проверить и донастроить `vite-plugin-pwa` (иконки, manifest, режимы обновления)
  - Добавить fallback‑страницу/сообщение на случай отсутствия данных

#### 📋 **service-info-screen** — _LOW, pending_
- **Итог:** есть экран «О приложении» с версией, источниками данных и дисклеймерами.
- **Работы:**
  - Создать отдельный экран/модалку и связать её с основным UI

#### 📋 **station-statuses** — _LOW, pending_
- **Итог:** приложение может отображать закрытые/частично закрытые станции и предупреждать пользователя о проблемных участках маршрута.
- **Работы:**
  - Определить формат статусов в данных (например, `station_statuses.json`: `open`, `temporarily_closed`, `partial`, `no_entry` и т.п.)
  - На уровне данных связать статусы с ID станций из `fullGraph.json`
  - Добавить визуальное отображение статусов на карте (иконки/обводка, отключение станции из кликов/маршрутов при полном закрытии)
  - Отмечать проблемные станции и участки в саммари маршрута (бейдж/предупреждение на карточке маршрута)

#### 📋 **station-card-and-exits** — _MEDIUM, pending_
- **Итог:** по каждой станции доступна карточка с основными данными: линии, выходы, краткая информация.
- **Работы:**
  - Определить формат карточки станции (JSON со списком выходов и краткими описаниями)
  - Реализовать панель/модалку Station Card, открываемую по клику на станцию или шаг маршрута
  - Показать в карточке базовую информацию: название, линии, список выходов; опционально — ссылку «Открыть в Яндекс.Картах/2ГИС»

#### 📋 **client-analytics** — _LOW, pending_
- **Итог:** есть минимальная анонимная аналитика использования (какие маршруты/станции популярны), без нарушения приватности.
- **Работы:**
  - Подключить лёгкий analytics‑слой или собственный сбор обезличенных событий

#### 📋 **i18n-basic** — _LOW, pending_
- **Итог:** базовая поддержка RU/EN (и, возможно, других языков) в UI и подписях.
- **Работы:**
  - Вынести строки UI в ресурсы
  - Продумать отображение названий станций на разных языках

#### 📋 **favorites** — _LOW, pending_
- **Итог:** пользователь может сохранять часто используемые маршруты/станции и быстро выбирать их.
- **Работы:**
  - Определить формат хранения избранного (localStorage/IndexedDB)
  - Добавить UI для добавления/выбора избранных пар станций
  - Интегрировать избранные станции/маршруты в подсказки `RouteForm` (блок «Избранные» над общим списком станций)

#### 📋 **recent-routes** — _MEDIUM, pending_
- **Итог:** последние маршруты всегда под рукой; повторный выбор занимает один‑два тапа.
- **Работы:**
  - Хранить последние N маршрутов в `localStorage` (пара станций + базовые параметры маршрута)
  - Показать блок «Недавние» в подсказках `RouteForm` (под списком избранных)
  - При выборе недавнего маршрута заполнять поля «Откуда/Куда» и строить маршрут заново

#### 📋 **nearby-stations** — _LOW, pending_
- **Итог:** приложение может предлагать ближайшие станции на основе текущего местоположения пользователя.
- **Работы:**
  - Использовать `navigator.geolocation` (при согласии пользователя) и `lat/lon` станций из `fullGraph.json`
  - Вычислять небольшой список ближайших станций и отображать их отдельным блоком «Рядом» в подсказках `RouteForm`
  - Обеспечить корректную работу при отсутствии разрешения или недоступности геолокации (graceful fallback)

#### 📋 **detailed-route-view** — _MEDIUM, pending_
- **Итог:** продвинутый экран маршрута с временной шкалой, оценкой времени в пути и удобным просмотром всех пересадок.
- **Работы:**
  - Спроектировать формат детализированных шагов маршрута
  - Реализовать отдельный экран/панель и связать с картой

### 12.7. Технический долг и инфраструктура

#### 🚧 **doc-readme-refactor** — _HIGH, in_progress_
- **Итог:** README задуман как актуальная и полезная точка входа для разработки.
- **Выполнено:**
  - ✅ Восстановлен детальный беклог
- **Осталось:**
  - 📋 Наладить регулярное обновление разделов при изменении пайплайна/архитектуры/алгоритмов

#### 📋 **pwa-cache-debug** — _MEDIUM, pending_
- **Итог:** локальный preview/PWA всегда показывает актуальную версию схемы после `npm run build` и `npm run optimize:layout`.
- **Работы:**
  - Проанализировать поведение service worker'а и кэш‑стратегии
  - При необходимости изменить `start_url`/настройки workbox для dev/preview

#### 📋 **ui-parity-yandex** — _MEDIUM, pending_
- **Итог:** ощущение от использования приложения близко к Яндекс.Метро: удобный выбор станции, маршрута, маршруты «по клику» на схеме.
- **Работы:**
  - Синхронизировать UX‑паттерны (альтернативные маршруты, гибкая шторка, удобная навигация по шагам)
  - Интегрировать `favorites` и `recent-routes` в подсказки `RouteForm` (блоки «Избранные» и «Недавние»)
  - Добавить настройку «Выбор станций тапом по схеме» и режимы `off/on` (тап по станции открывает поповер «Откуда/Куда» или сразу заполняет активное поле)
  - Довести поведение bottom‑sheet (открытие/закрытие, свайпы, анимации) до референса Яндекс.Метро

#### 📋 **multi-city-support** — _LOW, pending_
- **Итог:** одно и то же приложение может работать с несколькими городами (несколько схем метро/МЦК/МЦД), пользователь выбирает город.
- **Работы:**
  - Продумать формат хранения нескольких графов (`fullGraph` на город)
  - Добавить выбор города при первом запуске и переключатель города в настройках
  - Обновить пайплайн данных под многогородовой режим

#### 📋 **mcd-integration** — _LOW, pending_
- **Итог:** МЦД (D1–D4) интегрированы в граф и отображаются на схеме вместе с метро и МЦК.
- **Работы:**
  - Расширить пайплайн данных (`metro.ru.csv`/доп. источники) для включения МЦД
  - Добавить линии и станции МЦД в `fullGraph.json` и на схему (отдельный стиль/цвета)
  - Убедиться, что маршрутизатор корректно учитывает пересадки между метро, МЦК и МЦД

#### 📋 **nfc-travel-card-balance** — _LOW, pending_
- **Итог:** на Android‑устройствах с NFC пользователь может просматривать баланс транспортной карты.
- **Работы:**
  - Исследовать доступные API/SDK для чтения транспортных карт по NFC и юридические ограничения
  - Спроектировать отдельный экран/раздел для проверки баланса
  - Реализовать безопасный и локальный (без передачи персональных данных) сценарий чтения баланса

### 12.8. Итеративный полу‑автоматический подбор параметров layout'а

✅ **Система автоматической оптимизации** — _HIGH, completed_

**Реализовано:**
- ✅ Скрипт автоматического поиска оптимальных параметров (`optimize_params_depth_search.ts`)
- ✅ Поиск в глубину с отсечением (branch and bound)
- ✅ Расширенный анализ качества схемы (`analyze_layout_advanced.ts`)
- ✅ Сохранение лучших параметров в `normalized/best_params.json`

**Рабочий процесс:**

1. **Определить функцию качества `E(layout)`**
   - Компоненты:
     - `E_octilinear` — штраф за отклонение углов сегментов от 0/45/90/135°
     - `E_bend` — суммарная кривизна линии (сумма квадратов углов поворота между сегментами)
     - `E_cross` — штраф за пересечения линий вне хабов
     - `E_spacing` — штраф за слишком маленькие/слишком большие расстояния между соседними станциями
     - `E_ring` — отклонение точек кольцевых линий от идеальной окружности
     - `E_hub` — компактность хабов (расстояние между станциями в хабе)
   - Общий скоринг реализован в `analyze_layout_advanced.ts`

2. **Автоматический поиск параметров**
   - Варианты параметров: радиусы колец, раздвижение, итерации сглаживания и оптимизации
   - Поиск в глубину с отсечением худших вариантов
   - Beam search для продолжения только с лучшими вариантами

3. **Цикл оптимизации**
   - Пересборка графа с новыми параметрами
   - Анализ метрик качества
   - Сохранение лучших параметров

4. **Сравнение версий**
   - Сохранение метрик в `layout_metrics.json`
   - Возможность сравнения разных конфигураций

---

Этот README — единая точка входа: здесь собраны архитектура, пайплайн данных, спецификация схемы и дорожная карта. При существенных изменениях алгоритма layout'а, структуры графа или поведения приложения README нужно актуализировать.
